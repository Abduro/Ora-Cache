[InternetShortcut]
url=https://registry.khronos.org/OpenGL/extensions/EXT/EXT_bgra.txt
desc=EXT_bgra extends the list of host-memory color formats.
desc_0=Specifically, it provides formats which match the memory layout of Windows DIBs so that applications can use the same data in both Windows API calls and OpenGL pixel API calls.