;
; there is a limitation for naming files of thiss object: the name must not contain 'con' part of the file name,
; for example: 'con.font.h' will lead VS to crash when trying to open this file for editing its code;
;